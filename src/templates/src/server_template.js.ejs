const express = require('express');
const cors = require('cors');
const routes = require('./routes.js');

server = express();

server.use(cors())
server.use(express.json());
server.use(routes)

door = 3334;

server.listen(door);

console.log(`server listening on door ${door}. Access http://localhost:${door} to view the site!`);